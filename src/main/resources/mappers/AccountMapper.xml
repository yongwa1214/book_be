<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.book.account.AccountMapper">

    <insert id="save">
        INSERT INTO members
        SET name = #{name}
        , login_id = #{loginId}
        , login_pw = #{loginPw}
    </insert>

    <select id="findByLoginId">
        SELECT member_id, login_pw, name
        FROM members
        WHERE login_id = #{loginId}
    </select>

    <select id="sameId">
        SELECT count(*)
        FROM members
        WHERE login_id = #{loginId}
    </select>

</mapper>